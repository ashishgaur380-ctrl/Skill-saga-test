# Firebase Functions

Privileged server operations will live here.

Initial planned functions:
- setPlatformRole
- bootstrapSuperAdmin
- academic CRUD
- audit logging

Deploy only after the Firebase CLI is authenticated and the Development project is selected.

Never commit service-account credentials or private keys.
