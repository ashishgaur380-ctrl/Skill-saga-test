import{getFirestore}from"firebase-admin/firestore";import{onCall,HttpsError,type CallableRequest}from"firebase-functions/v2/https";
function auth(r:CallableRequest<unknown>){const uid=r.auth?.uid,role=r.auth?.token.role;if(!uid)throw new HttpsError("unauthenticated","Authentication required.");if(role!=="teacher")throw new HttpsError("permission-denied","Teacher access required.");return uid}
export const getTeacherDashboard=onCall(async r=>{const uid=auth(r),db=getFirestore();const [u,a,attempts,materials]=await Promise.all([db.collection("users").doc(uid).get(),db.collection("assignments").where("createdBy","==",uid).limit(500).get(),db.collection("quizAttempts").limit(1000).get(),db.collection("learningMaterials").where("createdBy","==",uid).limit(500).get()]);const ud=u.data()||{};const classes=new Set<string>(),students=new Set<string>();a.docs.forEach(d=>{const x=d.data();if(x.targetType==="class")classes.add(String(x.targetId));if(x.targetType==="learner")students.add(String(x.targetId));});let correct=0,total=0;attempts.docs.forEach(d=>{const x=d.data();correct+=Number(x.correct)||0;total+=Number(x.total)||0});return{teacher:{uid,displayName:String(ud.displayName||""),email:String(ud.email||"")},classesCount:classes.size,studentsCount:students.size,assignmentsCount:a.size,materialsCount:materials.size,averageAccuracy:total?Math.round(correct/total*100):0}});