import{getFirestore,FieldValue}from"firebase-admin/firestore";import{onCall,HttpsError,type CallableRequest}from"firebase-functions/v2/https";
const ROLES=new Set(["super_admin","admin","support","moderator","teacher","school_admin"]);const text=(v:unknown)=>typeof v==="string"?v.trim():"";
function auth(r:CallableRequest<unknown>){const uid=r.auth?.uid,role=r.auth?.token.role;if(!uid)throw new HttpsError("unauthenticated","Authentication required.");if(typeof role!=="string"||!ROLES.has(role))throw new HttpsError("permission-denied","Notification access denied.");return{uid,role};}
export const sendNotification=onCall(async r=>{const{uid,role}=auth(r);const p=r.data as any;const title=text(p?.title),message=text(p?.message),audience=text(p?.audience).toLowerCase(),targetIds=Array.isArray(p?.targetIds)?p.targetIds.map(text).filter(Boolean):[];if(!title||!message)throw new HttpsError("invalid-argument","Title and message are required.");if(!["all","learners","parents","teachers","targeted"].includes(audience))throw new HttpsError("invalid-argument","Invalid audience.");const db=getFirestore();let users:any[]=[];if(audience==="targeted")users=targetIds.map(id=>({id}));else{const roles=audience==="learners"?["learner"]:audience==="parents"?["parent"]:audience==="teachers"?["teacher"]:["learner","parent","teacher"];const snap=await db.collection("users").where("role","in",roles).limit(1000).get();users=snap.docs.map(d=>({id:d.id}));}if(users.length>1000)throw new HttpsError("resource-exhausted","Notification audience is too large.");const batch=db.batch();for(const u of users){batch.set(db.collection("notifications").doc(),{learnerId:u.id,title,message,type:text(p?.type)||"announcement",createdAt:FieldValue.serverTimestamp(),createdBy:uid,createdByRole:role,readAt:null});}await batch.commit();return{sent:users.length,channel:"in_app"};});
