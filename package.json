{
  "name": "skill-saga-2",
  "private": true,
  "version": "0.1.0",
  "description": "Skill Saga 2.0 — A smarter way to learn",
  "packageManager": "pnpm@10.0.0",
  "scripts": {
    "build:functions": "pnpm --dir functions build",
    "build:learner": "pnpm --dir apps/learner build",
    "build:admin": "pnpm --dir apps/admin build",
    "build": "pnpm build:functions && pnpm build:learner && pnpm build:admin",
    "typecheck": "pnpm build:functions",
    "test": "node scripts/validate-foundation.mjs && node scripts/validate-scheduling.mjs && node scripts/validate-security.mjs && node scripts/validate-production-hardening.mjs && node scripts/validate-admin-learner-regression.mjs && node scripts/validate-competition-integrity.mjs && node scripts/validate-automation-leaderboard-integrity.mjs && node scripts/validate-guardian-security.mjs && node scripts/validate-learning-content.mjs && node scripts/validate-quiz-integrity.mjs && node scripts/validate-competition-integrity-ui.mjs && node scripts/validate-content-access.mjs && node scripts/validate-assignments.mjs && node scripts/validate-ecosystem.mjs",
    "lint": "echo 'lint configured per app in CI'",
    "emulators:start:persistent": "mkdir -p emulator-data && npx firebase emulators:start --import=./emulator-data --export-on-exit=./emulator-data"
  },
  "workspaces": [
    "apps/*",
    "functions"
  ],
  "engines": {
    "node": ">=20 <21"
  }
}
