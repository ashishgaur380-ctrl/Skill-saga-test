# Shared Contracts

Shared contracts are the single TypeScript representation of data exchanged between applications and backend services.

Packages planned:
- types
- validation
- ui
- firebase
- domain

The server remains authoritative even when a validation contract is shared with clients.
