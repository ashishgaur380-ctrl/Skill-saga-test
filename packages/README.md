# Shared Packages

Planned shared packages:
- ui
- types
- validation
- config
- firebase
- domain

Business-critical validation may be shared for UX, but server-side validation remains authoritative.
