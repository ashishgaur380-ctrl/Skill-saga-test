"use client";import{useEffect,useState}from"react";import{onAuthStateChanged}from"firebase/auth";import{teacherAuth}from"./lib/firebase";import{teacherFunction}from"./lib/api";
export default function Teacher(){const[tab,setTab]=useState("dashboard"),[data,setData]=useState<any>({}),[error,setError]=useState("");
useEffect(()=>onAuthStateChanged(teacherAuth,async u=>{if(!u)return;try{const t=await u.getIdToken();setData(await teacherFunction("getTeacherDashboard",{},t))}catch(e){setError(e instanceof Error?e.message:"Unable to load dashboard.")}}),[]);
return <main className="teacher-shell"><header><div className="brand">Skill Saga <span>Teacher</span></div><p>Teach smarter. Track progress. Inspire learning.</p></header><nav>{["dashboard","classes","students","assignments","content","analytics"].map(x=><button className={tab===x?"active":""} onClick={()=>setTab(x)} key={x}>{x}</button>)}</nav>{error&&<div className="error">{error}</div>}<section><h1>{tab==="dashboard"?"Teacher Dashboard":tab[0].toUpperCase()+tab.slice(1)}</h1>{tab==="dashboard"?<div className="grid"><article><b>{data.classesCount??0}</b><span>Classes</span></article><article><b>{data.studentsCount??0}</b><span>Students</span></article><article><b>{data.assignmentsCount??0}</b><span>Assignments</span></article><article><b>{data.averageAccuracy??0}%</b><span>Average Accuracy</span></article></div>:<div className="panel">This teacher workspace is connected to the Skill Saga backend. {tab==="assignments"&&"Create and monitor learner/class assignments."} {tab==="content"&&"Upload learning materials and quizzes."} {tab==="analytics"&&"View class and student performance."}</div>}</section></main>}