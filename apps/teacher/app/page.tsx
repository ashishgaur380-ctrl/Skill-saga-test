"use client";
import{useEffect,useState}from"react";import{onAuthStateChanged,signInWithEmailAndPassword,signOut}from"firebase/auth";import{teacherAuth}from"./lib/firebase";import{teacherFunction}from"./lib/api";
export default function Teacher(){const[tab,setTab]=useState("dashboard"),[data,setData]=useState<any>({}),[user,setUser]=useState<any>(null),[email,setEmail]=useState(""),[password,setPassword]=useState(""),[busy,setBusy]=useState(false),[error,setError]=useState("");
useEffect(()=>onAuthStateChanged(teacherAuth,async u=>{setUser(u);if(!u)return;try{const t=await u.getIdToken();setData(await teacherFunction("getTeacherDashboard",{},t))}catch(e){setError(e instanceof Error?e.message:"Unable to load dashboard.")}}),[]);
async function login(e:any){e.preventDefault();setError("");setBusy(true);try{await signInWithEmailAndPassword(teacherAuth,email.trim(),password)}catch(e){setError(e instanceof Error?e.message:"Sign in failed.")}finally{setBusy(false)}}
if(!user)return <main className="teacher-login"><section className="login-card"><div className="brand">Skill Saga <span>Teacher</span></div><h1>Teacher Sign In</h1><p>Manage classes, assignments and student learning.</p><form onSubmit={login}><input type="email" placeholder="Teacher email" value={email} onChange={e=>setEmail(e.target.value)} required/><input type="password" placeholder="Password" value={password} onChange={e=>setPassword(e.target.value)} required/><button className="primary" disabled={busy}>{busy?"Signing in…":"Sign In"}</button></form>{error&&<div className="error">{error}</div>}</section></main>;
return <main className="teacher-shell"><header><div className="brand">Skill Saga <span>Teacher</span></div><p>Teach smarter. Track progress. Inspire learning. <button onClick={()=>signOut(teacherAuth)}>Sign out</button></p></header><nav>{["dashboard","classes","students","assignments","content","tests","analytics","reports","profile"].map(x=><button className={tab===x?"active":""} onClick={()=>setTab(x)} key={x}>{x}</button>)}</nav>{error&&<div className="error">{error}</div>}<section><h1>{tab==="dashboard"?"Teacher Dashboard":tab[0].toUpperCase()+tab.slice(1)}</h1>{tab==="dashboard"?<div className="grid"><article><b>{data.classesCount??0}</b><span>Classes</span></article><article><b>{data.studentsCount??0}</b><span>Students</span></article><article><b>{data.assignmentsCount??0}</b><span>Assignments</span></article><article><b>{data.averageAccuracy??0}%</b><span>Average Accuracy</span></article></div>:<div className="panel"><h2>{tab==="assignments"?"Assignments":tab==="content"?"Learning Content":tab==="tests"?"Tests & Schedule":tab==="analytics"?"Performance Analytics":tab==="reports"?"Class Reports":tab==="profile"?"Teacher Profile":tab[0].toUpperCase()+tab.slice(1)}</h2><p>This workspace is connected to the Skill Saga backend. The selected teacher module is ready for its data-management controls.</p>{tab==="assignments"&&<p>Create learner, class or school assignments from the central assignment manager; teacher ownership is enforced by the backend role.</p>}{tab==="analytics"&&<p>Dashboard accuracy and assignment metrics are already available through the teacher service.</p>}</div>}</section></main>}