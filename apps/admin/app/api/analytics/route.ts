import{NextRequest,NextResponse}from"next/server";const projectId=process.env.GCLOUD_PROJECT??"skill-saga-2";
const region=process.env.FIREBASE_FUNCTIONS_REGION??"us-central1";
const base=process.env.NEXT_PUBLIC_USE_FIREBASE_EMULATORS==="true"?`http://127.0.0.1:5001/${projectId}/${region}`:(process.env.FIREBASE_FUNCTIONS_BASE_URL??`https://${region}-${projectId}.cloudfunctions.net`);
export async function POST(req:NextRequest){const authorization=req.headers.get("authorization");if(!authorization)return NextResponse.json({error:{message:"Authentication is required."}},{status:401});try{const r=await fetch(`${base}/getAnalyticsSummary`,{method:"POST",headers:{"Content-Type":"application/json",Authorization:authorization},body:JSON.stringify({data:{}}),cache:"no-store"});const t=await r.text();let p:any;try{p=JSON.parse(t)}catch{p={error:{message:t||"Invalid Functions response."}}}if(!r.ok)return NextResponse.json(p,{status:r.status});return NextResponse.json({data:p?.data??p},{status:r.status})}catch(e){return NextResponse.json({error:{message:e instanceof Error?e.message:"Unable to reach Firebase Functions."}},{status:502})}}