import{NextRequest,NextResponse}from"next/server";const allowed=new Set(["createLearnerLinkCode","linkLearner","listLinkedLearners","getLinkedLearnerProgress"]);const projectId=process.env.GCLOUD_PROJECT??"skill-saga-2";export async function POST(req:NextRequest){const authorization=req.headers.get("authorization");if(!authorization)return NextResponse.json({error:{message:"Authentication is required."}},{status:401});const body=await req.json();if(!allowed.has(body.action))return NextResponse.json({error:{message:"Unsupported action."}},{status:400});try{const r=await fetch(`http://127.0.0.1:5001/${projectId}/us-central1/${body.action}`,{method:"POST",headers:{"Content-Type":"application/json",Authorization:authorization},body:JSON.stringify({data:body.data??{}})});const p=await r.json();return NextResponse.json(p,{status:r.status})}catch(e){return NextResponse.json({error:{message:e instanceof Error?e.message:"Unable to reach Functions."}},{status:502})}}