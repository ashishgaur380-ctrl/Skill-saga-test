"use client";
type Item={id:string;name:string};
export default function LearningSelectors({boards,classes,boardId,classId,onBoard,onClass,visibleClasses}:{boards:Item[];classes:Item[];boardId:string;classId:string;onBoard:(id:string)=>void;onClass:(id:string)=>void;visibleClasses:Item[]}){return <div className="ss-selects"><select value={boardId} onChange={e=>onBoard(e.target.value)} aria-label="Board"><option value="">Choose Board</option>{boards.map(x=><option key={x.id} value={x.id}>{x.name}</option>)}</select><select value={classId} onChange={e=>onClass(e.target.value)} aria-label="Class"><option value="">Choose Class</option>{visibleClasses.map(x=><option key={x.id} value={x.id}>{x.name}</option>)}</select></div>;}