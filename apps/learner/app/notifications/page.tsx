"use client";
import{useEffect,useState}from"react";import{onAuthStateChanged}from"firebase/auth";import{learnerAuth}from"../../lib/firebase";import{learnerFunction}from"../../lib/learner-api";import LearnerNav from"../components/LearnerNav";
export default function Notifications(){const[items,setItems]=useState<any[]>([]),[token,setToken]=useState(""),[error,setError]=useState("");
useEffect(()=>onAuthStateChanged(learnerAuth,async u=>{if(!u)return;try{const t=await u.getIdToken();setToken(t);const x=await learnerFunction("listLearnerNotifications",{},t);setItems(x?.items||[])}catch(e){setError(e instanceof Error?e.message:"Unable to load notifications.")}}),[]);
async function read(id:string){if(!token)return;try{await learnerFunction("markNotificationRead",{id},token);setItems(a=>a.map(x=>x.id===id?{...x,readAt:new Date().toISOString()}:x))}catch(e){setError(e instanceof Error?e.message:"Unable to update notification.")}}
function openAction(x:any){if(x.actionUrl) window.location.href=x.actionUrl;}
return <div className="ss-app"><header className="ss-inner-header"><a href="/">‹</a><div><b>Notifications</b><small>Stay updated on your learning journey</small></div><span>🔔</span></header><main className="ss-page"><div className="ss-heading"><b>Your updates</b><span>{items.filter(x=>!x.readAt).length} unread</span></div>{error&&<div className="ss-message error">{error}</div>}<div className="ss-notification-list">{items.length?items.map(x=><div key={x.id} className={"ss-notification "+(!x.readAt?"unread":"")} onClick={()=>void read(x.id)}><span>🔔</span><div><b>{x.title||"Skill Saga update"}</b><p>{x.message||""}</p><small>{x.createdAt?.seconds?new Date(x.createdAt.seconds*1000).toLocaleString():"New"}</small>{x.actionUrl&&<a className="ss-notification-action" href={x.actionUrl} onClick={()=>void read(x.id)}>{x.actionLabel||"Open"}</a>}</div></div>):<div className="ss-empty"><strong>No notifications yet</strong><span>Important Skill Saga updates will appear here.</span></div>}</div></main><LearnerNav active="Home"/></div>}
