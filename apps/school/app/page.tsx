"use client";import{useEffect,useState}from"react";import{onAuthStateChanged}from"firebase/auth";import{schoolAuth}from"./lib/firebase";import{schoolFunction}from"./lib/api";export default function School(){const[d,setD]=useState<any>({}),[e,setE]=useState("");useEffect(()=>onAuthStateChanged(schoolAuth,async u=>{if(!u)return;try{setD(await schoolFunction("getSchoolDashboard",{},await u.getIdToken()))}catch(x){setE(x instanceof Error?x.message:"Unable to load dashboard.")}}),[]);return <main className="shell"><div className="brand">Skill Saga · School</div><p>School operations, teachers, classes and student performance.</p>{e&&<p>{e}</p>}<div className="grid"><div className="card"><b>{d.teachersCount??0}</b>Teachers</div><div className="card"><b>{d.studentsCount??0}</b>Students</div><div className="card"><b>{d.classesCount??0}</b>Classes</div><div className="card"><b>{d.assignmentsCount??0}</b>Assignments</div></div></main>}